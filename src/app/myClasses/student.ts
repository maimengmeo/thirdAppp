export class Student {
    sid!: number;
    sname!: string;
    slogin!: string;
    scampus!: string;
}

//!: postfix which tells Angular that this will never be null
//can also just create the class and include a constructor